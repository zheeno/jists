# Efezino Ukpowe — Newsletter (GitHub Pages)

This directory is a **generated** GitHub Pages site.

- Draft issues are generated into: `../output/`
- The site is generated into: this folder
- A future step can push to GitHub (requires a GitHub token / credentials)

## How to publish (manual)
1. Create a GitHub repo (public) named `efezinoukpowe-newsletter`.
2. Copy this folder contents into the repo (or `git init` + push).
3. Enable **GitHub Pages** for the `main` branch.

If you want, I can generate a `git` command bundle once the repo exists.
